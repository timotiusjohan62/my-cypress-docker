# Docker Cypress API Test Setup

This repo includes:

- Node.js + Express backend
- PostgreSQL DB (in Docker)
- Cypress tests (fully Dockerized)
- Docker Compose setup

## Run everything:

```bash
docker compose up --build
